import { Stack } from "expo-router"

export default ()=>{
    return <Stack
        screenOptions={{
            headerShown: false,
            animation:"slide_from_right",
        }}
    >

    </Stack>
}